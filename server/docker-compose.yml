# «Живой экран» — два контейнера: app (Node + SQLite + папка data/) и caddy (HTTPS, статика).
#   cp .env.example .env && docker compose up -d --build
#   curl http://localhost:8080/healthz
# На VPS то же самое: в .env SITE_ADDRESS=домен (punycode для кириллицы), CADDY_HTTP_PORT=80, CADDY_HTTPS_PORT=443.
name: living-screen

services:
  app:
    build: .
    restart: unless-stopped
    # PID 1 — настоящий init, а не node: зависший healthcheck внутри контейнера
    # иначе оставляет зомби-процесс до перезапуска
    init: true
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      # База и детские рисунки. Обычная папка, а не docker-том: бэкап — архивом, переезд — копированием.
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    env_file: .env
    ports:
      # локально 8080 и 8443, на VPS в .env: CADDY_HTTP_PORT=80, CADDY_HTTPS_PORT=443
      - "${CADDY_HTTP_PORT:-8080}:80"
      - "${CADDY_HTTPS_PORT:-8443}:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./www:/srv/www:ro
      - caddydata:/data
      - caddyconfig:/config
    depends_on:
      - app

volumes:
  caddydata:
  caddyconfig:
